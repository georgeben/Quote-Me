<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="@dimen/save_quote_dialog_padding">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/save_quote_dialog_title"
        android:text="@string/save_quote_dialog_title"
        android:textSize="21sp"
        android:textStyle="bold"
        android:layout_gravity="center"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/save_quote_dialog_message"
        android:text="@string/save_quote_message"
        android:textSize="17sp"
        android:layout_marginTop="@dimen/save_quote_dialog_message_padding_top"/>


    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/quoteNameEditText"
        android:hint="Enter the name of the quote here"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:weightSum="2">
        <Button
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@drawable/rounded_button"
            android:id="@+id/save_quote_button"
            android:text="Save Quote"
            android:textColor="@color/colorPrimary"
            android:textAllCaps="false"
            android:layout_margin="5dp"/>

        <Button
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:id="@+id/cancel_dialog_button"
            android:text="Cancel"
            android:textColor="@color/colorPrimary"
            android:background="@drawable/rounded_button"
            android:textAllCaps="false"
            android:layout_margin="5dp"/>

    </LinearLayout>
</LinearLayout>